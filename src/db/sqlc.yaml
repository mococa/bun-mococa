version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "migrations/"
    gen:
      typescript:
        out: "generated/"
        emit_json_tags: true
        json_tags_case_style: "camel"